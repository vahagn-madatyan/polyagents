---
phase: 02-market-discovery-and-pipeline-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/connectors/sports_data.py
  - tests/test_sports_data_connector.py
  - .env.example
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, MKT-03]
user_setup:
  - service: api-sports
    why: "Team statistics, season performance, head-to-head matchup history"
    env_vars:
      - name: SPORTS_DATA_API_KEY
        source: "https://api-sports.io — sign up, Dashboard > Account > API Key"
  - service: the-odds-api
    why: "Bookmaker odds for value-bet divergence detection"
    env_vars:
      - name: SPORTS_ODDS_API_KEY
        source: "https://the-odds-api.com — sign up, Dashboard > API Key"

must_haves:
  truths:
    - "Bot fetches current season win rates for both teams before each game"
    - "Bot fetches head-to-head matchup records between teams"
    - "Bot fetches external bookmaker odds and compares against Polymarket price"
    - "Bot detects value bets when odds diverge beyond configurable threshold"
    - "Team stats are cached for 24 hours; odds are cached for 5 minutes"
    - "Sports without SPORTS_REQUIRE_STATS coverage can trade without stats"
  artifacts:
    - path: "agents/connectors/sports_data.py"
      provides: "SportsDataConnector class with API-Sports and The Odds API integration"
      min_lines: 150
    - path: "tests/test_sports_data_connector.py"
      provides: "Unit tests for stats, H2H, odds, caching, and divergence detection"
      min_lines: 100
  key_links:
    - from: "agents/connectors/sports_data.py"
      to: "API-Sports endpoints"
      via: "httpx GET with x-apisports-key header"
      pattern: "api-sports\\.io|x-apisports-key"
    - from: "agents/connectors/sports_data.py"
      to: "The Odds API endpoints"
      via: "httpx GET with apiKey param"
      pattern: "the-odds-api\\.com|odds-api"
    - from: "agents/connectors/sports_data.py"
      to: "cachetools.TTLCache"
      via: "two-tier caching (stats 24h, odds 5min)"
      pattern: "TTLCache"
---

<objective>
Build the SportsDataConnector that integrates with API-Sports for team statistics and head-to-head records, and The Odds API for bookmaker odds divergence detection.

Purpose: Provide the external data layer that feeds Phase 3's LLM prompts with team stats, win rates, H2H records, and odds divergence signals. Without this, the bot trades blind on Polymarket price alone.
Output: SportsDataConnector class with two-tier TTL caching, configurable per-sport stats requirement, odds divergence detection, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/djbeatbug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/djbeatbug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-CONTEXT.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From agents/connectors/sports_ws.py (env helper pattern):
```python
def _env_int(key: str, default: int) -> int:
    return int(os.environ.get(key, str(default)))
# Also _env_bool, _env_float variants — inline in new files to avoid heavy imports
```

From agents/utils/objects.py:
```python
class SportGameState(BaseModel):
    game_id: int
    league: str
    slug: str
    home_team: str
    away_team: str
    # ... rest of fields
```

Logging pattern (from sports_ws.py):
```python
print(f"[sports_data] event=stats_fetch league={league} team={team}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SportsDataConnector with team stats, H2H, and caching</name>
  <files>agents/connectors/sports_data.py, tests/test_sports_data_connector.py, .env.example</files>
  <behavior>
    - Test: get_team_stats returns win_rate, wins, losses, recent_form for a valid team+league (mock API-Sports 200)
    - Test: get_team_stats returns None when API key is missing (graceful degradation)
    - Test: get_head_to_head returns list of past matchup results for two teams (mock API-Sports H2H endpoint)
    - Test: get_head_to_head returns empty list on 404 or error
    - Test: team stats cache returns cached value within 24h TTL without second API call
    - Test: odds cache returns cached value within 5min TTL without second API call
    - Test: get_external_odds returns bookmaker odds for a given sport+teams (mock The Odds API)
    - Test: detect_value_bet returns True when |polymarket_price - implied_odds_prob| > threshold
    - Test: detect_value_bet returns False when divergence is within threshold
    - Test: detect_value_bet uses configurable SPORTS_ODDS_DIVERGENCE_THRESHOLD (default 0.05)
    - Test: sport not in SPORTS_REQUIRE_STATS list returns None from get_team_stats without API call
    - Test: _map_league_to_api_sport maps "nfl" -> correct API-Sports sport key, "nba" -> correct key, unknown -> None
  </behavior>
  <action>
    1. Create `agents/connectors/sports_data.py` with `SportsDataConnector` class:
       - `__init__`: load env vars (`SPORTS_DATA_API_KEY`, `SPORTS_ODDS_API_KEY`, `SPORTS_REQUIRE_STATS`, `SPORTS_STATS_CACHE_TTL_SECONDS=86400`, `SPORTS_ODDS_CACHE_TTL_SECONDS=300`, `SPORTS_ODDS_DIVERGENCE_THRESHOLD=0.05`). Use inlined `_env_int`, `_env_float` helpers (same pattern as sports_ws.py — do NOT import from executor.py). Create two `cachetools.TTLCache` instances: `_stats_cache` (maxsize=500, ttl from env) and `_odds_cache` (maxsize=200, ttl from env). Create `httpx.Client` with reasonable timeout (10s).
       - `_map_league_to_api_sport(self, league: str) -> Optional[str]`: mapping dict from Polymarket league strings to API-Sports sport identifiers (e.g., "nfl" -> "american-football", "nba" -> "basketball", "mlb" -> "baseball", "nhl" -> "hockey", "soccer" -> "football", "cbb"/"cfb" -> respective endpoints). Return None for unmapped (esports, etc.)
       - `_requires_stats(self, league: str) -> bool`: check if league is in `SPORTS_REQUIRE_STATS` list
       - `get_team_stats(self, league: str, team_name: str) -> Optional[dict]`: if not `_requires_stats`, return None. Check `_stats_cache` first. Call API-Sports team standings/statistics endpoint. Parse response into dict with keys: `win_rate`, `wins`, `losses`, `draws`, `recent_form` (last 5 results). Cache result. Return None on error with log.
       - `get_head_to_head(self, league: str, home_team: str, away_team: str) -> list[dict]`: check `_stats_cache`. Call API-Sports H2H endpoint. Parse into list of dicts with `date`, `home_score`, `away_score`, `winner`. Cache. Return empty list on error.
       - `get_external_odds(self, league: str, home_team: str, away_team: str) -> Optional[dict]`: check `_odds_cache`. Call The Odds API for sport, parse response for matching event. Return dict with `home_odds`, `away_odds`, `draw_odds`, `implied_home_prob`, `implied_away_prob`, `source_bookmaker`. Cache. Return None on error.
       - `detect_value_bet(self, polymarket_price: float, implied_prob: float) -> bool`: return `abs(polymarket_price - implied_prob) > self._divergence_threshold`
       - `get_game_context(self, game_state: "SportGameState") -> dict`: convenience method combining stats for both teams + H2H + odds into a single dict for LLM prompt injection. Returns partial data if some sources fail.
       - Use `tenacity.retry` with `stop_after_attempt(3)` and `wait_exponential(min=1, max=10)` on API call methods for transient failures.

    2. Add env vars to `.env.example` (append, do not overwrite existing):
       ```
       # External sports data APIs
       SPORTS_DATA_API_KEY=""
       SPORTS_ODDS_API_KEY=""
       SPORTS_REQUIRE_STATS="nfl,nba,mlb,nhl"
       SPORTS_STATS_CACHE_TTL_SECONDS="86400"
       SPORTS_ODDS_CACHE_TTL_SECONDS="300"
       SPORTS_ODDS_DIVERGENCE_THRESHOLD="0.05"
       ```

    3. Write tests in `tests/test_sports_data_connector.py` following TDD. Mock all HTTP calls via `unittest.mock.patch` on `httpx.Client.get`. Provide realistic mock API-Sports and Odds API response fixtures. Verify cache behavior by asserting call counts.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/polyagents && python -m pytest tests/test_sports_data_connector.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>SportsDataConnector class exists with get_team_stats, get_head_to_head, get_external_odds, detect_value_bet, and get_game_context methods. Two-tier TTL caching works (24h stats, 5min odds). SPORTS_REQUIRE_STATS controls per-sport opt-in. Divergence threshold is configurable. All unit tests pass with mocked HTTP responses.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_sports_data_connector.py -x -v` — all tests pass
- `grep -c "class SportsDataConnector" agents/connectors/sports_data.py` returns 1
- `grep -c "TTLCache" agents/connectors/sports_data.py` returns 2+
- `grep "SPORTS_DATA_API_KEY\|SPORTS_ODDS_API_KEY\|SPORTS_REQUIRE_STATS\|SPORTS_ODDS_DIVERGENCE_THRESHOLD" .env.example` returns all 4
</verification>

<success_criteria>
- SportsDataConnector provides team stats, H2H, and external odds via API-Sports and The Odds API
- Two-tier TTL caching prevents excessive API calls (24h stats, 5min odds)
- Value-bet detection works with configurable divergence threshold (default 5%)
- Sports not in SPORTS_REQUIRE_STATS can trade without external stats
- All unit tests pass with fully mocked HTTP responses
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-discovery-and-pipeline-architecture/02-02-SUMMARY.md`
</output>
