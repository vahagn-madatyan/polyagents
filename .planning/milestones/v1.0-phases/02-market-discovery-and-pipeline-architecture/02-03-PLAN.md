---
phase: 02-market-discovery-and-pipeline-architecture
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - agents/application/budget.py
  - agents/sports.py
  - tests/test_budget_coordinator.py
  - tests/test_sports_pipeline.py
  - .env.example
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "Sports pipeline runs as a separate process via python -m agents.sports"
    - "BudgetCoordinator uses filelock for cross-process race condition protection"
    - "SPORTS_BUDGET_FRACTION env var controls sports budget share"
    - "Per-sport caps are configurable via SPORTS_CAP_{LEAGUE} env vars"
    - "Sports pipeline supports dry-run mode via SPORTS_EXECUTE_TRADES override"
    - "A crash in sports pipeline does not terminate the general pipeline"
    - "Safety floor stops sports trades when wallet drops below SPORTS_MIN_WALLET_USD"
  artifacts:
    - path: "agents/application/budget.py"
      provides: "BudgetCoordinator class with filelock cross-process coordination"
      contains: "class BudgetCoordinator"
    - path: "agents/sports.py"
      provides: "Sports pipeline entry point"
      contains: "def main"
    - path: "tests/test_budget_coordinator.py"
      provides: "Unit tests for budget coordination, locking, per-sport caps"
      min_lines: 80
    - path: "tests/test_sports_pipeline.py"
      provides: "Unit tests for pipeline entry point and dry-run flag"
      min_lines: 40
  key_links:
    - from: "agents/sports.py"
      to: "agents/connectors/sports_ws.py"
      via: "SportsWSConnector import"
      pattern: "from agents\\.connectors\\.sports_ws import"
    - from: "agents/sports.py"
      to: "agents/polymarket/gamma.py"
      via: "GammaMarketClient import for slug table"
      pattern: "from agents\\.polymarket\\.gamma import"
    - from: "agents/sports.py"
      to: "agents/connectors/sports_data.py"
      via: "SportsDataConnector import"
      pattern: "from agents\\.connectors\\.sports_data import"
    - from: "agents/sports.py"
      to: "agents/application/budget.py"
      via: "BudgetCoordinator import"
      pattern: "from agents\\.application\\.budget import.*BudgetCoordinator"
    - from: "agents/application/budget.py"
      to: "filelock"
      via: "FileLock for cross-process budget safety"
      pattern: "from filelock import|filelock\\.FileLock"
---

<objective>
Implement BudgetCoordinator with cross-process locking and the sports pipeline entry point that wires together the WS connector, slug table, data connector, and budget management.

Purpose: Enable the sports pipeline to run as an independent process alongside the general trading pipeline with budget safety guarantees. This is the architectural backbone that Phase 3 (pre-game analysis) and Phase 4 (live trading) build on top of.
Output: BudgetCoordinator class using filelock for cross-process coordination, sports pipeline entry point (`python -m agents.sports`), dry-run mode support, and unit tests for both.
</objective>

<execution_context>
@/Users/djbeatbug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/djbeatbug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-CONTEXT.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-RESEARCH.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-01-SUMMARY.md
@.planning/phases/02-market-discovery-and-pipeline-architecture/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (created in this phase) -->
From agents/utils/objects.py:
```python
class SportsMarketTag(BaseModel):
    slug: str
    league: str
    home_team: str
    away_team: str
    market_id: str
    condition_id: str
    token_id_yes: str
    token_id_no: str
    question: str
    outcome_prices: Optional[str] = None
```

From agents/polymarket/gamma.py (new methods):
```python
class GammaMarketClient:
    def build_slug_table(self, game_states: dict) -> tuple[dict, list[str]]: ...
    def lookup_single_slug(self, ws_slug: str) -> list[SportsMarketTag]: ...
```

<!-- From Plan 02 (created in this phase) -->
From agents/connectors/sports_data.py:
```python
class SportsDataConnector:
    def get_game_context(self, game_state: SportGameState) -> dict: ...
    def detect_value_bet(self, polymarket_price: float, implied_prob: float) -> bool: ...
```

<!-- From Phase 1 -->
From agents/connectors/sports_ws.py:
```python
class SportsWSConnector:
    def get_all_game_states(self) -> dict[int, SportGameState]: ...
    def get_game_state(self, game_id: int) -> Optional[SportGameState]: ...
    def get_message_queue(self) -> queue.Queue: ...
    def run(self) -> None: ...
    def stop(self) -> None: ...

def should_halt_trading(game_state: SportGameState) -> bool: ...
```

From agents/application/budget.py (existing):
```python
class SessionBudgetManager:
    def __init__(self, total_budget: float) -> None: ...
    def remaining(self) -> float: ...
    def can_spend(self, amount: float) -> bool: ...
    def record_trade(self, amount: float, market_id: int, outcome: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BudgetCoordinator with filelock cross-process coordination</name>
  <files>agents/application/budget.py, tests/test_budget_coordinator.py, .env.example</files>
  <behavior>
    - Test: BudgetCoordinator.__init__ reads SPORTS_BUDGET_FRACTION and calculates sports allocation from total wallet balance
    - Test: allocate_budget acquires filelock before writing budget allocation to JSON file
    - Test: get_sports_budget returns allocated amount for sports pipeline
    - Test: get_general_budget returns remaining amount (total - sports allocation)
    - Test: can_spend_sports returns False when amount exceeds remaining sports budget
    - Test: can_spend_sports returns False when wallet balance below SPORTS_MIN_WALLET_USD
    - Test: get_sport_cap returns per-sport cap for configured leagues (e.g., SPORTS_CAP_NFL=0.4 -> 40% of sports budget)
    - Test: get_sport_cap returns equal share of remainder for unconfigured leagues
    - Test: record_sports_trade decrements sports budget under filelock
    - Test: filelock timeout raises after SPORTS_BUDGET_LOCK_TIMEOUT_SECONDS (default 5s)
    - Test: concurrent access simulation — two BudgetCoordinator instances do not corrupt shared JSON
  </behavior>
  <action>
    1. Add `BudgetCoordinator` class to `agents/application/budget.py` (below existing `SessionBudgetManager`):
       - `__init__(self, wallet_balance: float)`: load env vars with inlined `_env_float`/`_env_int` helpers (do NOT import from executor.py). Read `SPORTS_BUDGET_FRACTION` (default 0.30), `SPORTS_MIN_WALLET_USD` (default 50.0), `SPORTS_BUDGET_LOCK_TIMEOUT_SECONDS` (default 5), `SPORTS_BUDGET_LOCK_PATH` (default "/tmp/polyagents_budget.lock"), `SPORTS_BUDGET_FILE_PATH` (default "/tmp/polyagents_budget.json"). Parse `SPORTS_CAP_{LEAGUE}` env vars into dict. Calculate allocations.
       - `allocate_budget(self) -> dict`: acquire `filelock.FileLock`, write allocation to JSON file (`{"sports": X, "general": Y, "per_sport": {...}, "wallet_balance": Z, "timestamp": ...}`). Return allocation dict.
       - `get_sports_budget(self) -> float`: read from JSON under lock
       - `get_general_budget(self) -> float`: read from JSON under lock
       - `can_spend_sports(self, amount: float, wallet_balance: float) -> bool`: check amount <= remaining sports budget AND wallet_balance >= SPORTS_MIN_WALLET_USD
       - `get_sport_cap(self, league: str) -> float`: return configured cap fraction * sports budget. Unconfigured leagues: `(1.0 - sum(configured_caps)) / num_unconfigured * sports_budget`
       - `record_sports_trade(self, amount: float, league: str) -> None`: acquire lock, decrement sports budget and per-sport allocation in JSON, release lock
       - `_read_budget_file(self) -> dict`: read JSON under lock
       - `_write_budget_file(self, data: dict) -> None`: write JSON under lock

    2. Add env vars to `.env.example`:
       ```
       # Budget coordination
       SPORTS_BUDGET_FRACTION="0.30"
       SPORTS_MIN_WALLET_USD="50.0"
       SPORTS_CAP_NFL="0.4"
       SPORTS_CAP_NBA="0.3"
       SPORTS_CAP_MLB="0.15"
       SPORTS_CAP_NHL="0.15"
       SPORTS_BUDGET_LOCK_TIMEOUT_SECONDS="5"
       SPORTS_BUDGET_LOCK_PATH="/tmp/polyagents_budget.lock"
       SPORTS_BUDGET_FILE_PATH="/tmp/polyagents_budget.json"
       ```

    3. Write tests in `tests/test_budget_coordinator.py`. Use `tmp_path` fixture for lock and JSON file paths (override env vars in test). Test concurrent access by creating two BudgetCoordinator instances pointing at same lock/JSON files.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/polyagents && python -m pytest tests/test_budget_coordinator.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>BudgetCoordinator class exists in budget.py with filelock-based cross-process coordination. Per-sport caps work. Safety floor prevents trades below minimum wallet balance. All unit tests pass including concurrent access test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sports pipeline entry point and dry-run mode</name>
  <files>agents/sports.py, agents/__main__.py, tests/test_sports_pipeline.py</files>
  <behavior>
    - Test: main() initializes SportsWSConnector, GammaMarketClient, SportsDataConnector, BudgetCoordinator
    - Test: main() builds initial slug table from WS game states
    - Test: main() respects SPORTS_EXECUTE_TRADES override (dry-run when False)
    - Test: main() falls back to EXECUTE_TRADES master flag when SPORTS_EXECUTE_TRADES not set
    - Test: main() runs idle loop when no games are active (does not crash)
    - Test: main() handles KeyboardInterrupt for clean shutdown
    - Test: pipeline can be invoked via `python -m agents.sports`
  </behavior>
  <action>
    1. Create `agents/sports.py` — the sports pipeline entry point:
       - `_resolve_dry_run() -> bool`: check `SPORTS_EXECUTE_TRADES` first (overrides); if not set, fall back to `EXECUTE_TRADES` master flag. Use inlined `_env_bool` helper. Log which flag is active.
       - `main()`:
         a. Log startup: `print(f"[sports_pipeline] event=startup dry_run={not execute_trades}")`
         b. Initialize `BudgetCoordinator(wallet_balance)` — for now, wallet_balance can be a placeholder (actual CLOB balance integration is Phase 3+; use env var `SPORTS_INITIAL_WALLET_USD` default 1000.0 for dev/test)
         c. Initialize `GammaMarketClient()`
         d. Initialize `SportsDataConnector()`
         e. Initialize `SportsWSConnector()` and start via `connector.run()` in a thread
         f. Wait for initial game states (sleep loop up to 30s for WS to connect and receive first data)
         g. Build initial slug table via `gamma_client.build_slug_table(connector.get_all_game_states())`
         h. Log slug table size and unmapped count
         i. Enter main loop: process message queue events, refresh slug table every `SPORTS_SLUG_REFRESH_INTERVAL_SECONDS`, log game state changes. The loop is the framework — actual trade logic is Phase 3.
         j. Handle `KeyboardInterrupt` → `connector.stop()`, log clean shutdown
       - `if __name__ == "__main__": main()` block

    2. Create or update `agents/__main__.py` if needed — ensure `python -m agents.sports` works as entry point. If `agents/__main__.py` already exists for general pipeline, do NOT modify it. The sports pipeline is invoked directly as `python -m agents.sports`.

    3. Write tests in `tests/test_sports_pipeline.py`. Mock all external dependencies (SportsWSConnector, GammaMarketClient, SportsDataConnector, BudgetCoordinator). Test that main() wires them together correctly and respects dry-run flags. Use `unittest.mock.patch` to prevent actual WS connections.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/polyagents && python -m pytest tests/test_sports_pipeline.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Sports pipeline entry point exists at agents/sports.py. `python -m agents.sports` is a valid invocation. Dry-run mode respects SPORTS_EXECUTE_TRADES override and EXECUTE_TRADES master flag. Pipeline initializes all Phase 2 components (WS connector, slug table, data connector, budget coordinator). Clean shutdown on KeyboardInterrupt. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_budget_coordinator.py tests/test_sports_pipeline.py -x -v` — all tests pass
- `grep -c "class BudgetCoordinator" agents/application/budget.py` returns 1
- `grep -c "FileLock\|filelock" agents/application/budget.py` returns 1+
- `grep "def main" agents/sports.py` returns the entry point
- `grep "SPORTS_EXECUTE_TRADES\|SPORTS_BUDGET_FRACTION\|SPORTS_MIN_WALLET_USD" .env.example` returns all 3
- `python -c "from agents.sports import main; print('import OK')"` succeeds (but may fail on missing deps — that is acceptable if the import chain is correct)
</verification>

<success_criteria>
- BudgetCoordinator uses filelock for cross-process safety with budget JSON file
- SPORTS_BUDGET_FRACTION controls the sports/general budget split
- Per-sport caps (SPORTS_CAP_{LEAGUE}) work correctly with equal-share remainder
- Safety floor (SPORTS_MIN_WALLET_USD) prevents trades when wallet is too low
- Sports pipeline entry point wires all Phase 2 components together
- Dry-run mode works via dual-flag system (SPORTS_EXECUTE_TRADES overrides EXECUTE_TRADES)
- Pipeline is crash-isolated from general pipeline (separate OS process)
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-discovery-and-pipeline-architecture/02-03-SUMMARY.md`
</output>
